
services:
  app:
    build: .
    container_name: jwt_app
    ports:
      - "5000:5000"
    volumes:
      - .:/app
      - /app/node_modules
    env_file:
      - .env
    environment:
        RUNNING_IN_DOCKER: "true"

    command: node index.js